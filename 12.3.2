const obj = {
  key1: 'value1',
  key2: 'value2',
  key3: 'value3',
  key4: 'value4',
}

let keyNum = 'key1'

const foo = (keyNum, obj) => {
  for(let key in obj) {
    if (key === keyNum) {
      console.log(key === keyNum)
    }
    else (console.log(false))
  }
};

foo('key2', obj);

// Решение не соответствует поставленной задаче. Функция должна возвращать true или false в зависимости от того, присутствует в объекте свойство с данным ключом или нет. Правильное решение выглядит так:

function checkPropInObj (str, obj) {
  return (str in obj);
}
